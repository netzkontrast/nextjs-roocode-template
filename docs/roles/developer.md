# Role: Developer

## Responsibilities
- Implement code according to design.
- Write unit and integration tests (AAA pattern).
- Ensure code passes all linters and build checks.

## Key Instructions
- Always read `docs/tech_stack.md`.
- Write tests *alongside* code (Colocation).
- Verify changes with `npm run test` and `npm run lint`.
